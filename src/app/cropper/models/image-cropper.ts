export interface ImageCropperInterface {
  additionalTitle: string;
  description: string;
  imageQuality: number;
  resizeTo: number;
  aspectRatio: any;
}

export interface ImageCropperUploadedInfoInterface {
  id: number;
  image: string;
  description: string;
}
